window.SAHAYATA_CONFIG = Object.freeze({
  // Local backend expected by the frontend contract. Replace this value at deployment time.
  apiBaseUrl: "http://127.0.0.1:8000",
  requestTimeoutMs: 20000,
});
